# SPDX-License-Identifier: Apache-2.0
name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  lint-and-test:
    name: fmt · clippy · build · test (default features)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Set up Rust (stable + rustfmt + clippy)
        run: |
          rustup toolchain install stable --profile minimal --component rustfmt --component clippy
          rustup default stable
      - uses: Swatinem/rust-cache@v2
      - name: rustfmt
        run: cargo fmt --all --check
      - name: clippy (deny warnings)
        run: cargo clippy --workspace --all-targets --locked -- -D warnings
      - name: build (default features)
        run: cargo build --workspace --locked
      - name: test (offline fixture/wire suite)
        run: cargo test --workspace --locked

  test-all-connectors:
    name: test every connector (gRPC + local)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      # gRPC providers (Google/NVIDIA STT+TTS) codegen .proto via tonic-build.
      # cmake + a C/C++ toolchain (for the local Whisper build) are preinstalled.
      - name: Install protoc
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler
      - name: Set up Rust (stable)
        run: |
          rustup toolchain install stable --profile minimal
          rustup default stable
      - uses: Swatinem/rust-cache@v2
      # `cargo test` compiles every connector AND runs its offline fixture/wire
      # tests (live tests are #[ignore]'d). These are feature-gated, so the
      # default-features job above does not reach them.
      - name: flowcat-core (vad-ort + filter-rnnoise)
        run: cargo test -p flowcat-core --locked --features vad-ort,filter-rnnoise
      - name: flowcat-services (every provider)
        run: cargo test -p flowcat-services --locked --features stt-all,tts-all,llm-all,realtime-all,obs-all,mcp,brain-http
      - name: flowcat-transports (every transport)
        run: cargo test -p flowcat-transports --locked --features webrtc-str0m,ws,daily,livekit,local
      - name: flowcat-telephony (every serializer)
        run: cargo test -p flowcat-telephony --locked --features twilio,telnyx,exotel,vonage,genesys,asterisk,cloudonix,vobiz,dtmf-inband
      # The config-driven server + the browser WebRTC playground are feature-gated
      # (axum/tokio/str0m), so the default-features job above does not reach them.
      # `webrtc` implies `server`, so this exercises both the HTTP server and the
      # playground, plus the full provider connector set the binary can run.
      - name: flowcat-server (server + webrtc playground)
        run: |
          cargo clippy -p flowcat-server --locked --features webrtc --all-targets -- -D warnings
          cargo test -p flowcat-server --locked --features webrtc
      # Guard the embedder seam (#30): the framework + WebRTC helper surface
      # (`webrtc::handle_offer`, `build_router`) MUST compile with NO
      # `flowcat-services/*-all` bundle, so an embedder can pair it with its own
      # curated connector set. If this breaks, the framework grew a hard connector
      # dependency it shouldn't have.
      - name: flowcat-server (webrtc-helper — no connector bundles)
        run: |
          cargo clippy -p flowcat-server --locked --features webrtc-helper --all-targets -- -D warnings
          cargo test -p flowcat-server --locked --features webrtc-helper
